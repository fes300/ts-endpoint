import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as stories from './react-editable-text.stories';
import EditableText from '.';

<Meta title="EditableText" component={EditableText} />

# EditableText

`EditableText` is a simple component that lets you modify text inline:

<Preview withSource="open">
  <Story name="basic">{stories.basic()}</Story>
</Preview>

Many input types and custom styles/structures are allowed:

<Preview>
  <Story name="types">{stories.typesGallery()}</Story>
</Preview>

You can also mirror the typed text somewhere else in your app:

<Preview>
  <Story name="mirror">{stories.mirrorText()}</Story>
</Preview>

<br />

<br />

## Props

<Props of={EditableText} />
