{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^9.0.1",
    "@commitlint/config-conventional": "^9.0.1",
    "babel-loader": "^8.1.0",
    "@types/jest": "^26.0.7",
    "husky": "^4.2.5",
    "lerna": "^3.22.1",
    "ts-loader": "8.0.3",
    "typescript": "4.0.2"
  },
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "release": "lerna publish --yes",
    "pre-release": "lerna publish --yes --conventional-prerelease",
    "test": "lerna run test"
  },
  "dependencies": {
    "fp-ts": "^2.6.7"
  }
}
