{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^12.0.0",
    "@commitlint/config-conventional": "^12.0.0",
    "@types/jest": "^26.0.7",
    "husky": "^5.2.0",
    "lerna": "^4.0.0",
    "typescript": "^4.2.3",
    "dts-jest": "^23.3.0",
    "jest": "^26.4.2",
    "ts-jest": "^26.5.4"
  },
  "scripts": {
    "postinstall": "husky install",
    "build": "lerna run build",
    "bootstrap": "yarn && lerna bootstrap",
    "release": "lerna publish --yes --conventional-graduate --force-publish --skip-npm",
    "pre-release": "lerna publish --yes --conventional-prerelease --npm-tag next --skip-npm",
    "test": "lerna run test"
  },
  "dependencies": {
    "fp-ts": "^2.9.0",
    "io-ts": "^2.2.13"
  }
}
