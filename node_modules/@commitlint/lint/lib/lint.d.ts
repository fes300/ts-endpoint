import { LintRuleConfig, LintOptions, LintRuleOutcome } from '@commitlint/types';
export default function lint(message: string, rawRulesConfig?: LintRuleConfig, rawOpts?: LintOptions): Promise<{
    valid: boolean;
    errors: LintRuleOutcome[];
    warnings: LintRuleOutcome[];
    input: string;
}>;
//# sourceMappingURL=lint.d.ts.map